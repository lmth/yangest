[package]
name = "yangest-plugin-tree"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
authors = ["Magnus Thoäng <magnusthoang@gmail.com>"]

[dependencies]
clap = { version = "4", default-features = false, features = ["std"] }
inventory = "0.3"
yangest-core = { path = "../../core" }
